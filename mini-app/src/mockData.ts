/**
 * mockData.ts — intentionally empty.
 * All data must come from real agent activity and backend sessions.
 * These exports are kept for type safety but contain no seed data.
 */
import type { Agent, TxRecord } from "./types";

export const SEED_AGENTS: Agent[] = [];
export const MOCK_ACTIVITY: TxRecord[] = [];
